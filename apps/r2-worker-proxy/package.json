{
  "name": "r2-worker-proxy",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker fronting one R2 bucket via a native binding, so apps/backend can reach R2 without depending on R2's S3 API endpoint's specific Cloudflare anycast prefix.",
  "type": "module",
  "scripts": {
    "pretest": "wrangler types",
    "test": "vitest run",
    "pretypecheck": "wrangler types",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.8",
    "typescript": "^5.7.3",
    "vitest": "^4.1.0",
    "wrangler": "^4.131.1"
  }
}
